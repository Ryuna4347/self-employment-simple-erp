import { z } from "zod"

/**
 * 비밀번호 유효성 검사 스키마
 * - 최소 8자
 * - 영문 + 숫자 + 특수문자(@$!%*?&) 포함
 */
export const passwordSchema = z
  .string()
  .min(8, "비밀번호는 8자리 이상 입력해야합니다.")
  .regex(
    /^(?=.*[a-zA-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$/,
    "비밀번호는 영문+숫자+특수문자 포함 8자리 이상 입력해야합니다.",
  )
